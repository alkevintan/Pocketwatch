<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Intentionally empty. This fork has no release feed yet, and pointing the updater at
         upstream would offer their APK (different signature and applicationId) as an update.
         Add this fork's own manifest URL here when releases start. -->
    <string-array name="update_urls" />
</resources>
